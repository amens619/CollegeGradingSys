@model IEnumerable<CollegeGradingSys.ViewModels.Nationality.NationalityIndexVM>

@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService authorizationService


@{
    ViewData["Title"] = "إدارة الدول والجنسيات";
    ViewData["PageName"] = "إدارة الدول والجنسيات";
    ViewData["SubPageName"] = "القائمة الرئسية";

}

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <p>
            @if ((await authorizationService.AuthorizeAsync(User, "CreateNationalityPolicy")).Succeeded)
            {
                <a asp-action="Create" class="btn btn-secondary  col-2 btn-sm "><i class="fas fa-plus mr-3"></i>جديد</a>
            }
        </p>
        <div class="card  col-md-10 mb-4">
            <div class="card-body">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col" class=" col  th-font-size text-center" style="vertical-align: middle;width: 40%;">
                                @Html.DisplayNameFor(model => model.CountryName)
                            </th>
                            <th scope="col" class=" col  th-font-size text-center" style="vertical-align: middle;width: 40%;">
                                @Html.DisplayNameFor(model => model.NationalityName)
                            </th>
                            <th style="width: 20%;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center td-font-size">
                                    @Html.DisplayFor(modelItem => item.CountryName)
                                </td>
                                <td class="text-center td-font-size">
                                    @Html.DisplayFor(modelItem => item.NationalityName)
                                </td>
                                <td class="text-center">
                                    @if ((await authorizationService.AuthorizeAsync(User, "EditNationalityPolicy")).Succeeded)
                                    {
                                        <a asp-action="Edit" class="btn btn-outline-success btn-sm align-self-center" asp-route-id="@item.Id">
                                            <i class="fas fa-edit" width="17" height="17"></i>
                                        </a>
                                    }
                                    @if ((await authorizationService.AuthorizeAsync(User, "DeleteNationalityPolicy")).Succeeded)
                                    {
                                        <a asp-action="Delete" class="btn btn-outline-danger btn-sm align-self-center" asp-route-id="@item.Id">
                                            <i class="fas fa-trash-alt" width="17" height="17"></i>
                                        </a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
    </div>
</section>