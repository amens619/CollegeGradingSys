@model CollegeGradingSys.ViewModels.BatchCreateData


@{
    ViewData["Title"] = "إضافة دفعة";
}

<div class="card mb-4">
    <div class="card-body">
        <div class="bg-white">
            <h5 class="mb-4">  <span style="margin-left:6px;" data-feather="plus"></span>إدارة الدفعات</h5>
            <h6 class="m-1">إضافة دفعة</h6>
            <hr class="mb-4 mt-0">
            <div class="row col-md-12">
                <form asp-action="Create">
                    @if (ViewBag.Message != null)
                    {
                        <h5 class="alert alert-danger">@ViewBag.Message</h5>
                    }
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="row">
                        <div class="form-group col-md-6">
                            <label asp-for="BatchName" class="control-label"></label>
                            <input asp-for="BatchName" class="form-control text-center"  data-val="true"  data-val-required="الرجاء ادخال اسم الدفعة" />
                            <span asp-validation-for="BatchName" class="text-danger"></span>
                        </div>
                        <div class="form-group col-md-6">
                            <label asp-for="SpecializationId" class="control-label"></label>
                            <select asp-for="SpecializationId" class="form-control text-center" asp-items="ViewBag.SpecializationId"></select>
                        </div>
                        <div class="form-group col-md-12">
                            <label asp-for="Note" class="control-label"></label>
                            <input asp-for="Note" class="form-control" />
                            <span asp-validation-for="Note" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group mt-4">
                        <input type="submit" value="إضافة" class="btn col-2 btn-primary btn-sm" />
                        <a class="btn btn-secondary  btn-sm" asp-action="Index">القائمة الرئسية</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
